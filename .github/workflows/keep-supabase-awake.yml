name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 8 */3 * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Vercel API
        run: |
          curl -fsSL https://dvf-api-flame.vercel.app/api/ping
